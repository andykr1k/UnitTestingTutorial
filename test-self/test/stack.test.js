describe('My Operations', () => {
    
it('Adding', () => {
    expect(55 + 5).toBe(60)
})

it('Subtracting', () => {
    expect(70 - 20).toBe(50);
})

it('Multiplying', () => {
    expect(70 * 2).toBe(140);
})

it('Dividing', () => {
    expect(70 / 2).toBe(35);
})

it('Modulus', () => {
    expect(70 % 3).toBe(1);
})

})